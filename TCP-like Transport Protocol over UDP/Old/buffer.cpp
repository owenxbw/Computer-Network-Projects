
#include <map>

std::map<int,string> buffer;  //client receiver window 15360 bytes?

if (){
	while(1){
		receivefrom;
		if seq!=;{
			buffer.insert(std::pair<int,string>(seq,buf));
		}
	}
	std::cout << "buffer contains:\n";
    for (it=mymap.begin(); it!=mymap.end(); ++it)
    std::cout << it->first << " => " << it->second << '\n';



if (out-of-order){
	buffer.insert(std::pair<int,string>(clientseq,bufstr.substr(pos+8)));

}

if (in-order){
	if(retransmission ==true){
		for (;;){
		at = buffer.find(++clientseq);                   //find all sequencial packets
		if(at!= buffer.end){
			outfile?
			send r     
			erase;
			                              
		}else{

		}                        //should 
	}
	send receive packet clientseq+1;        //accumulative ack!!   
	retransmission=false;
	} 
}